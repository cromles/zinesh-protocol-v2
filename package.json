{
  "name": "zinesh-v2",
  "version": "2.0.0",
  "private": true,
  "description": "Zinesh Protocol V2 — event-sourced escrow kernel",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --runInBand",
    "test:coverage": "node --experimental-vm-modules node_modules/.bin/jest --runInBand --coverage"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.0.0",
    "@types/pg": "^8.23.1",
    "jest": "^29.7.0",
    "pg": "^8.23.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.5.0"
  }
}
